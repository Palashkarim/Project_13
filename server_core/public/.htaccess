<IfModule mod_rewrite.c>
  RewriteEngine On

  # If the request is not an existing file or directory, redirect to index.php
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.php [QSA,L]
</IfModule>
